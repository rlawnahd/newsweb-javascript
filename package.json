{
  "dependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "start": "scripts start",
    "build": "scripts build",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build"
  }
}
